import { MovieDetail } from "./detail/MovieDetail";

type CreditsProps = {
  movieId: string;
};

export const Credits = ({movieId}
    : CreditsProps )=>{

    // const [crew,setCrew] =useState<>()
}